{
  "buildCommand": "cd wimate-app && npm install && npm run build",
  "outputDirectory": "wimate-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
