{
  "framework": "vite",
  "installCommand": "cd wimate-app && npm install --legacy-peer-deps",
  "buildCommand": "cd wimate-app && npm run build",
  "outputDirectory": "wimate-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
